name: coverage_lens
description: Static HTML coverage reports for Dart and Flutter LCOV output with compact source previews and threshold checks.
version: 1.3.0
homepage: https://github.com/lacopiroty/coverage_lens
repository: https://github.com/lacopiroty/coverage_lens
issue_tracker: https://github.com/lacopiroty/coverage_lens/issues

environment:
  sdk: ^3.0.0

executables:
  coverage_lens:

platforms:
  linux:
  macos:
  windows:

topics:
  - coverage
  - lcov
  - testing
  - cli
  - flutter

dependencies:
  args: ^2.7.0
  image: ^4.5.4
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
